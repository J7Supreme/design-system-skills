# ds-refactor-agent

Orchestrates the refactoring of a design system based on audit findings. It harmonizes token structures, repairs accessibility violations, and generates dark mode token sets.

## Phase 2
This agent transforms raw or problematic tokens into a clean, normalized structure.

## Usage
```bash
python scripts/run_pipeline.py refactor --run-id <PREVIOUS_AUDIT_ID>
```
